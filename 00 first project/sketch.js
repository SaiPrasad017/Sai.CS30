// Project Title
// Your Name
// Date
// one line description

//interactive program


function setup() {
  //this happends one at the start
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  //happened over and over (60fps)
  background(220);
  circle(mouseX, mouseY, 30);
}
